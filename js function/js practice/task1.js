/**
 * Take four parameters. Multiply the four numbers and then return the result
 */
function multiplyFourNumber(num1, num2, num3, num4){
    const mul = num1 * num2 * num3 * num4;
    return mul;
}
const result = multiplyFourNumber(2, 3, 4, 5);
console.log(result);