const first = 'jan';
const last = 'pakhi';

const name = first + ' ' + last;
console.log(name);

const a = 10;
const b = 20;
const result = 'The sum ' + a + ' and ' + b + ' is ' + (a+b);
console.log(result);

// another way to do this

const math = `The sum of ${a} and ${b} is ${a + b}`;
console.log(math);

// template string
const challenge = `Johan Cehan
cehan chena
lage toke`;
console.log(challenge);